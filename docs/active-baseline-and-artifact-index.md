# Vicion Bot V3 — Active Baseline & Artifact Index

Generated by TICKET-160 (repository cleanup/inventory); corrected by TICKET-160A (documentation accuracy +
evidence tracking, see `data/ticket160a-final-summary.md`). Points at the CURRENT authoritative
config/evidence artifacts after T147-T159 research and T160/T160A cleanup. See
`memory/project_official_backtest_config.md` for the historical PM-confirmed CLI flags themselves (still
the correct 8 flags to pass); see `data/ticket160-deferred-issues.md` item 2 for why the exact trade-count
figure in that memory note is now stale (superseded by the already-merged T152 guard, unrelated to T160).
See `docs/baseline-registry.md` (TICKET-160A) for the three distinctly-named locked-in baselines — they
must NOT be conflated as a single "active baseline."

## 0. OB (Order Block) conclusion — corrected by TICKET-160A

```text
T157: OB_DISABLE_EXPERIMENT — không V2 candidate nào đạt winner gate; không đổi production.
T158: SHADOW_ONLY — hướng OB-disabled cải thiện Central nhưng chưa đủ holdout để merge/deploy.
Production default tại input commit: obEnabled=true.
Research baseline T159: sử dụng OB-disabled counterfactual; không phải production baseline.
```

Verified word-for-word against `docs/archive/ticket157/ticket157-ob-v2-architecture-challenger-validation-report.md`
("Decision: OB_DISABLE_EXPERIMENT ... No V2 candidate passes the winner gate; no production change is
authorized.") and `docs/archive/ticket158/ticket158-formal-ob-disabled-portfolio-experiment-report.md`
("Decision: SHADOW_ONLY. The disable direction is favorable, but existing-history holdout remains too thin
for merge authorization."). `obEnabled=true` confirmed in `apps/bot/src/entry/entryRouter.ts`
`DEFAULT_ENTRY_ROUTER_CONFIG`. T159's frozen baseline population is explicitly the OB-disabled Central
counterfactual (`data/archive/ticket157/ticket157-ob_disabled-central-ledger.csv`), not production
behavior — see `docs/baseline-registry.md` `RESEARCH_OB_DISABLED_CENTRAL_T158_T159`.

## 1. Production defaults currently in effect

- `obEnabled` (entry router OB detector): default `true` (OB-enabled) — unchanged production behavior.
  See `apps/bot/src/entry/entryRouter.ts` `DEFAULT_ENTRY_ROUTER_CONFIG`.
- `sameSideDuplicateGuardEnabled` (T152): default enabled (blocks same-side duplicate positions per
  symbol) unless explicitly set to `false`. See `apps/bot/src/orchestrator/orchestrator.ts`
  `shouldBlockSameSideDuplicate`.
- `momentumEntryTimingResearch` (T153-T159 shadow wiring): default `undefined` = fully inert, falls
  through to the pre-existing `tryMomentumDirect` path byte-for-byte. See
  `apps/bot/src/orchestrator/orchestrator.ts` / `types.ts`.
- Official live CLI/env config: the 8-flag backtest command in
  `memory/project_official_backtest_config.md`, plus `apps/bot/scripts/liveRunner.ts`'s own `CONFIG`
  constant for live-only additions (`obSlBufferAtrMultiplier=0.87`, `momentumDirectMinSlPercent=1.27`,
  real-$100-capital risk scaling).

## 2. Current authoritative evidence, by ticket

| Ticket | Conclusion | Authoritative artifact(s) |
|---|---|---|
| T153B | Current-production execution-cost baseline (Fee-only/Light/Central/Conservative scenarios) | `data/archive/ticket153b/ticket153b-run-manifest.json` + `ticket153b-{scenario}-summary.json`/`-ledger.csv`/`-equity-drawdown.csv`, `ticket153b-scenario-summary.csv`, `ticket153b-cost-decomposition.csv`, `ticket153b-equity-curves.svg` |
| T154 | Live execution telemetry schema + shadow-calibration groundwork | `docs/ticket154-execution-telemetry-schema.md` (schema), `apps/bot/src/live/executionTelemetry.ts` (implementation, KEEP_ACTIVE, wired into `liveRunner.ts` behind `EXECUTION_TELEMETRY_ENABLED`), `apps/bot/src/live/fixtures/ticket154-sanitized-sample.jsonl` (sanitized sample) |
| T157 | **OB_DISABLE_EXPERIMENT** — no V2 candidate passes the winner gate; no production change authorized (production stays OB v1/`obEnabled=true`) | `docs/archive/ticket157/ticket157-ob-v2-architecture-challenger-validation-report.md`, `data/archive/ticket157/ticket157-analysis-summary.json`, `ticket157-v1-parity.csv`, `ticket157-sensitivity.csv`, `ticket157-ob_v1_current-*` / `ticket157-ob_v2_*-*` ledgers+summaries |
| T158 | **SHADOW_ONLY** — OB-disabled direction improves Central, but holdout is too thin for merge/deploy; no production change authorized (`obEnabled=true` unchanged) | `docs/archive/ticket158/ticket158-formal-ob-disabled-portfolio-experiment-report.md`, `data/archive/ticket158/ticket158-run-manifest.json`, `ticket158-regression-rollback.csv`, `ticket158-scenario-portfolio-comparison.csv` |
| T159 | Unified entry-timing optimization on the OB-disabled research counterfactual (NOT production) — final result: **KEEP_CURRENT_ENTRY** (no timing-research variant adopted) | `docs/archive/ticket159/ticket159-unified-entry-timing-optimization-report.md`, `data/archive/ticket159/ticket159-run-manifest.json`, `ticket159-scenario-portfolio-comparison.csv`, `ticket159-parity-leakage-report.md` + `.csv`, `ticket159-field-dictionary.md` |
| T152 (pre-T160, already merged) | Block same-side duplicate positions per symbol | `apps/bot/src/orchestrator/orchestrator.ts` (`shouldBlockSameSideDuplicate`), `apps/bot/src/orchestrator/orchestrator.test.ts`, `apps/bot/src/orchestrator/replayCompatibility.test.ts` |
| Balance sync (pre-T160, already merged, commit `89ceab5`) | Exchange-authoritative balance for Telegram messages | `apps/bot/src/live/liveBalanceSync.ts` + `liveBalanceSync.test.ts` |

## 3. Historically-valuable but non-authoritative (ARCHIVE)

Intermediate/exploratory audit outputs from T147, T147a, T148, T149, T150, T153, T153a, T153c, T156a,
T156b — preserved in full under `data/archive/<ticket>/` (moved, not deleted, not individually
re-verified for redundancy — see `data/ticket160-repository-inventory.csv` for the CLN-B rationale) and
`apps/bot/scripts/ticket<N>*.ts` for their generating scripts (left in `apps/bot/scripts/`, now tracked in
git).

## 4. Repository layout after T160

```
apps/bot/src/          production source (entry, regime, risk, xgbFilter, orchestrator, live, telegram,
                        backtest — the last two now include the T153-T159 files that turned out to be
                        load-bearing production imports, not pure research)
apps/bot/scripts/       backtest.ts, liveRunner.ts (production) + ticket147-159 one-off research scripts
                        (ARCHIVE, tracked in git, left in place)
data/archive/ticket<N>/ per-ticket research/evidence artifacts (T147-T159)
docs/archive/ticket<N>/ per-ticket final .md reports (T153-T159)
docs/ticket154-execution-telemetry-schema.md   T154 schema doc
docs/active-baseline-and-artifact-index.md     this file
```

Note: this repository's `.gitignore` excludes `docs/`, `data/`, and `README.md` by default (pre-existing
convention). **TICKET-160A adds a narrow allowlist** (see `data/ticket160a-evidence-allowlist.csv`) so this
file, `docs/baseline-registry.md`, the T154 schema doc, the T157/T158/T159 final decision reports, and a
minimum set of small T153B/T158/T159/T160 manifests/summaries/CSVs ARE tracked in git going forward — a
fresh clone now carries the minimum evidence needed to trust the baseline numbers in this document. Raw
OHLCV, live telemetry, `.env`, and whole `data/archive/`/`docs/archive/` directories remain untracked by
design (see `.gitignore` for the exact allowlist patterns).
